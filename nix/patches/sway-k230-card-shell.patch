--- a/include/sway/tree/container.h
+++ b/include/sway/tree/container.h
@@ -68,1 +68,2 @@
 struct sway_container {
+	bool card_shell_ordinary_maximized;
--- a/sway/tree/view.c
+++ b/sway/tree/view.c
@@ -1,3 +1,4 @@
+#include "sway/card_shell.h"
 #include <stdlib.h>
 #include <strings.h>
 #include <wayland-server-core.h>
@@ -970,6 +971,7 @@
 }
 
 void view_unmap(struct sway_view *view) {
+	card_shell_unmap(view);
 	wl_signal_emit_mutable(&view->events.unmap, view);
 
 	view->executed_criteria->length = 0;
--- a/sway/desktop/xdg_shell.c
+++ b/sway/desktop/xdg_shell.c
@@ -1,3 +1,4 @@
+#include "sway/card_shell.h"
 #include <float.h>
 #include <stdbool.h>
 #include <stdlib.h>
@@ -310,6 +311,7 @@
 		return;
 	}

+	card_shell_observe(view);
 	struct wlr_box *new_geo = &xdg_surface->geometry;
 	bool new_size = new_geo->width != view->geometry.width ||
 			new_geo->height != view->geometry.height ||
@@ -334,6 +336,7 @@
 		view_center_and_clip_surface(view);
 	}

+	card_shell_commit(view);
 	if (view->container->node.instruction) {
 		bool successful = transaction_notify_view_ready_by_serial(view,
 				xdg_surface->current.configure_serial);
--- a/sway/desktop/layer_shell.c
+++ b/sway/desktop/layer_shell.c
@@ -1,1 +1,2 @@
+#include "sway/card_shell.h"
 #include <stdbool.h>
@@ -94,0 +95,1 @@
+	card_shell_keyboard_adjust_usable(output, &usable_area);
@@ -98,1 +100,2 @@
-		arrange_output(output);
+		arrange_output(output);
+		card_shell_usable_area_changed(output);
@@ -221,0 +224,1 @@
+	layer->mapped = false; // The scene node is dying before nested arrange_layers.
--- a/sway/desktop/output.c
+++ b/sway/desktop/output.c
@@ -1,3 +1,5 @@
+#include "sway/card_shell.h"
+#include "sway/card_shell_telemetry.h"
 #include <assert.h>
 #include <stdlib.h>
 #include <strings.h>
@@ -187,6 +189,7 @@
 		wl_event_source_timer_update(timer->frame_done_timer, delay);
 	} else {
 		wlr_scene_surface_send_frame_done(scene_surface, &data->when);
+		card_shell_frame(buffer);
 	}
 }
 
@@ -275,7 +278,9 @@
 		return 0;
 	}
 
+	card_bench_render_begin(output);
 	output_configure_scene(output, &root->root_scene->tree.node, 1.0f);
+	card_shell_prepare(output);
 
 	struct wlr_scene_output_state_options opts = {
 		.color_transform = output->color_transform,
@@ -283,6 +288,7 @@
 
 	struct wlr_scene_output *scene_output = output->scene_output;
 	if (!wlr_scene_output_needs_frame(scene_output)) {
+		card_bench_render_end(output, false);
 		return 0;
 	}
 
@@ -290,6 +296,8 @@
 	wlr_output_state_init(&pending);
+	card_bench_render_stage(output, CARD_BENCH_BUILD);
 	if (!wlr_scene_output_build_state(output->scene_output, &pending, &opts)) {
 		wlr_output_state_finish(&pending);
+		card_bench_render_end(output, false);
 		return 0;
 	}
 
@@ -303,7 +310,10 @@
 		}
 	}
 
-	if (!wlr_output_commit_state(output->wlr_output, &pending)) {
+	card_bench_commit_begin(output);
+	bool committed = wlr_output_commit_state(output->wlr_output, &pending);
+	card_bench_render_end(output, committed);
+	if (!committed) {
 		sway_log(SWAY_ERROR, "Page-flip failed on output %s", output->wlr_output->name);
 	}
 	wlr_output_state_finish(&pending);
@@ -366,6 +376,7 @@
 		wl_event_source_timer_update(output->repaint_timer, delay);
 	}
 
+	card_shell_prepare(output);
 	// Send frame done to all visible surfaces
 	struct send_frame_done_data data = {0};
 	clock_gettime(CLOCK_MONOTONIC, &data.when);
@@ -469,11 +480,13 @@
 static void handle_present(struct wl_listener *listener, void *data) {
 	struct sway_output *output = wl_container_of(listener, output, present);
 	struct wlr_output_event_present *output_event = data;
+	card_bench_present(output, output_event);
 
 	if (!output->enabled || !output_event->presented) {
 		return;
 	}
 
+	card_shell_present(output, output_event->presented);
 	output->last_presentation = output_event->when;
 	output->refresh_nsec = output_event->refresh;
 }
--- a/sway/input/seat.c
+++ b/sway/input/seat.c
@@ -1,3 +1,4 @@
+#include "sway/card_shell.h"
 #include <assert.h>
 #include <linux/input-event-codes.h>
 #include <string.h>
@@ -956,6 +957,7 @@
 
 void seat_remove_device(struct sway_seat *seat,
 		struct sway_input_device *input_device) {
+	card_shell_cancel(seat);
 	struct sway_seat_device *seat_device = seat_get_device(seat, input_device);
 
 	if (!seat_device) {
@@ -1576,12 +1578,14 @@
 
 void seatop_touch_motion(struct sway_seat *seat, struct wlr_touch_motion_event *event,
 		double lx, double ly) {
+	if (card_shell_motion(seat, event->touch, event->touch_id, lx, ly, event->time_msec)) return;
 	if (seat->seatop_impl->touch_motion) {
 		seat->seatop_impl->touch_motion(seat, event, lx, ly);
 	}
 }
 
 void seatop_touch_up(struct sway_seat *seat, struct wlr_touch_up_event *event) {
+	if (card_shell_up(seat, event->touch, event->touch_id, event->time_msec)) return;
 	if (seat->seatop_impl->touch_up) {
 		seat->seatop_impl->touch_up(seat, event);
 	}
@@ -1589,12 +1593,14 @@
 
 void seatop_touch_down(struct sway_seat *seat, struct wlr_touch_down_event *event,
 		double lx, double ly) {
+	if (card_shell_down(seat, event->touch, event->touch_id, lx, ly, event->time_msec)) return;
 	if (seat->seatop_impl->touch_down) {
 		seat->seatop_impl->touch_down(seat, event, lx, ly);
 	}
 }
 
 void seatop_touch_cancel(struct sway_seat *seat, struct wlr_touch_cancel_event *event) {
+	if (card_shell_cancel(seat)) return;
 	if (seat->seatop_impl->touch_cancel) {
 		seat->seatop_impl->touch_cancel(seat, event);
 	}
--- a/sway/meson.build
+++ b/sway/meson.build
@@ -1,4 +1,14 @@
 sway_sources = files(
+	'card_shell.c',
+	'card-keyboard-gesture.c',
+	'card_shell_appearance.c',
+	'card_shell_route.c',
+	'card-shell-policy.c',
+	'card_shell_render.c',
+	'card_shell_icon.c',
+	'card_shell_scaled_cache.c',
+	'card_shell_telemetry.c',
+	'card_shell_test_input.c',
 	'commands.c',
 	'config.c',
 	'criteria.c',
--- a/sway/commands.c
+++ b/sway/commands.c
@@ -114,6 +114,7 @@
 static const struct cmd_handler command_handlers[] = {
 	{ "allow_tearing", cmd_allow_tearing },
 	{ "border", cmd_border },
+	{ "card_shell", cmd_card_shell },
 	{ "create_output", cmd_create_output },
 	{ "exit", cmd_exit },
 	{ "floating", cmd_floating },
--- a/include/sway/commands.h
+++ b/include/sway/commands.h
@@ -149,6 +149,7 @@
 sway_cmd cmd_input;
 sway_cmd cmd_seat;
 sway_cmd cmd_ipc;
+sway_cmd cmd_card_shell;
 sway_cmd cmd_kill;
 sway_cmd cmd_layout;
 sway_cmd cmd_log_colors;
--- a/sway/tree/output.c
+++ b/sway/tree/output.c
@@ -1,3 +1,4 @@
+#include "sway/card_shell.h"
 #include <assert.h>
 #include <ctype.h>
 #include <string.h>
@@ -277,6 +278,7 @@
 }
 
 void output_disable(struct sway_output *output) {
+	card_shell_output_disable(output);
 	if (!sway_assert(output->enabled, "Expected an enabled output")) {
 		return;
 	}
--- a/sway/server.c
+++ b/sway/server.c
@@ -0,0 +1 @@
+#include "sway/card_shell_test_input.h"
@@ -716,0 +718 @@
+	card_shell_test_input_finish();
