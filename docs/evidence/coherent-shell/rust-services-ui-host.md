# Rust Settings and notification shade source checkpoint

Source base: `6c9822b24c0949fed26abf6c94635080ed3dcc12` (2026-09-24). The opt-in Rust client consumes the already packaged `k230-settings` command and private notification broker through the bounded `ServiceWorker`; no blocking service I/O runs in a Wayland touch callback. The shade renders broker history and its separately redacted preview. Settings renders reported network, brightness, keyboard, and motion capability states, including unavailable labels. Power actions require the backend's confirmation token. Finger scrolling follows a touch in the notification list; a horizontal swipe dismisses an eligible row. Header gestures dismiss the panel; opening Settings remains within the same overlay.

Host verification from `nix/rust-shell-client`: `cargo fmt --all`, `cargo clippy --offline --lib --bin k230-shell-rust -- -D warnings`, `cargo test --offline -q`, and `git diff --check` passed. Cargo test covered 23 library, 4 binary, 10 appearance, 5 background, and 8 service-data tests. The new focused tests cover bounded panel targets, cancelled tap/scroll distinction, and the visible power-row gating when settings capability loading has not succeeded. These are source and host checks only.

Remaining gates: exact RISC-V package build from the committed source; paired compositor/client QEMU touch and service fixture showing shade history/preview, Settings unavailable/writable controls, notifications actions, and confirmation cancellation; physical finger and display review on 568×1232 glass. The current device image build and board are operated separately by the coordinator. No task requiring those checks is claimed complete here.
