All tasks are initially open. Commands naming new tools, tests or flake outputs
below are **planned interfaces**, to be added by that task before invocation;
they are not claims that those interfaces exist or have passed today. Host,
cross-build and physical proof are separate. Store committed results under
`docs/evidence/omarchy-themes/` with revisions, commands and limitations.

## 1. Reuse upstream helpers and unchanged source fixtures

- [x] 1.1 Pin and package the upstream palette, template, legacy-palette and OSC helpers plus relevant templates and licenses; carry the narrow staging/template-path patch and verify its output parity, then verify the closure with `nix build .#omarchy-theme-tools` and record the output. Reserve the sole build slot.
- [x] 1.2 Add exact-revision fixture acquisition for built-in light/dark themes and the community section-override sample; include `.git` directory clones, `.git` file worktrees and source directories without Git metadata. Record source hashes/licenses and prove no checkout changes with `python3 tests/test_omarchy_theme_sources.py`.
- [x] 1.3 Add helper compatibility tests for aliases, explicit ANSI/selection/custom colors, mode fallbacks, mix/gradient substitutions, full-file and section replacement precedence; compare upstream outputs with `python3 tests/test_omarchy_theme_resolution.py`.
  - [x] 1.4 Built-in catalog breadth slice: install every member of the pinned `omacom/omarchy` `themes/` collection (22, verified with `ls themes/` against the fetched source) into `nix/handheld-theme-default`'s `$out/share/omarchy/themes`, not only `catppuccin`/`catppuccin-latte`. Fixed a real parser gap this surfaced: three built-ins (hackerman, last-horizon, solitude) set `hyprland_active_border`/`hyprland_inactive_border` to a multi-stop Hyprland `rgba(RRGGBBAA)`/`rgb(RRGGBB)` gradient, which neither `tools/theme_tokens.py`'s `_argb` nor the Rust receiver's `palette_value` (`nix/rust-shell-client/src/appearance.rs`) accepted; both are now fixed with new unit tests (`tests/test_handheld_theme_rendering.py::test_hyprland_hex_gradient_and_bare_rgb_are_distinct_from_css_decimal_form`, `nix/rust-shell-client/tests/appearance_module.rs::builtin_multi_stop_hyprland_gradient_palette_uses_first_stop_color`), and all 22 built-ins now resolve. Measured upstream backgrounds at 66.4MB raw across all 22 themes (53MB backgrounds) against a 568x1232 panel with a compact-image requirement (`openspec/specs/image/sd-layout/spec.md`); 20 of the 22 (all but `catppuccin`/`catppuccin-latte`, whose bytes and generation identities are pinned) now ship a build-time `imagemagick mogrify -resize 'x1250>' -quality 82 -strip` background derivative at the same relative path, since every upstream background is landscape/square and the runtime decoder already crops to exact output size on every decode. This keeps the "unchanged theme checkout" requirement's contract (no source-checkout mutation at use-time, generated output lives outside it) — see `nix/handheld-theme-default/SOURCE.md` — without a spec change; only background pixels are a derivative, not palette/icon/metadata. Verified with `python3 tests/test_handheld_theme_bundle.py --package $(nix build --no-link --print-out-paths --max-jobs 1 --cores 4 .#handheld-theme-default)`, which now also runs every built-in through the real `theme_activate.prepare()` path (`check_all_builtins_resolve`) and asserts every bounded background is <= 1300px tall in the installed package. Measured closure: 28.7MB (`nix path-info -S`) for all 22 bounded, a single self-contained store path, versus 66.4MB unresized — 58% smaller for 11x the theme count of the previous 2-theme package. No panel/real-finger review of the new themes' readability exists yet; that is task group 5's reserved-board gate, unchanged by this slice.

Proof: `nix build .#omarchy-theme-tools` is a package cross-build; the two Python invocations are host checks only. No device claim.

## 2. Adapt the swap coordinator and shell notification

- [ ] 2.1 Implement compatible `omarchy-theme-set NAME` and paths plus direct source/collection selection, using upstream helpers and a narrow coordinator; test clone preservation, legacy scratch conversion, path boundaries and unknown-key reporting with `python3 tests/test_omarchy_theme_activation.py`.
- [ ] 2.2 Bridge upstream palette/shell/background payloads to our clients and replace desktop-only reload commands; test generation ordering, concurrent swaps, failed acknowledgement, rollback, update invalidation and removal of a source with `python3 tests/test_omarchy_theme_activation.py`.
- [ ] 2.3 Provide the pinned Nix default, fresh-home startup and persistent optional selections; build `nix build .#handheld-theme-service` and record its output and source revision.

Proof: `python3 tests/test_omarchy_theme_activation.py` and `nix build .#handheld-theme-service`. Host failure injection and package construction do not prove physical rollback or reboot behavior.

## 3. Apply all corresponding surface colors and icons

- [ ] 3.1 Build the generated-token consumer and coverage inventory for all upstream fields; verify real gradient stops/alpha, per-side widths, references, control states and font/spacing adaptations with `python3 tests/test_handheld_theme_rendering.py` and reviewed host captures.
- [ ] 3.2 Integrate drawer and card chrome with the shared generation and selected icon theme; verify actual source icon inheritance/cache invalidation plus dark/light captures with `python3 tests/test_handheld_theme_rendering.py --surface drawer-card`. Coordinate with existing icon/card worktrees.
- [ ] 3.3 Integrate Settings, notifications, chooser and keyboard as those sibling clients land; verify every corresponding role and explicit absent-surface report with `python3 tests/test_handheld_theme_rendering.py --surface system`. Do not tick this while consumers are only mockups.
  - [x] 3.3.k Keyboard slice only: derive wvkbd's seven start-up colour flags (`--bg/--fg/--fg-sp/--press/--press-sp/--text/--text-sp`) from the resolved palette in `tools/keyboard_appearance.py`; the supervised launcher (`nix/shell.nix` `k230-supervised-keyboard`) reads the published `keyboard-appearance/active/wvkbd.args` (falling back to a packaged default) and a persisted `k230-keyboard-visible` marker so a colour-triggered restart keeps shown/hidden state without a post-restart signal race; `theme_activate.py`/`theme_catalog.py` call `keyboard_appearance.sync_and_restart` right after the existing app-appearance post-commit call, reporting `keyboard_appearance` independently of shell/app status. Restart only ever signals a same-uid `wvkbd-mobintl` when the launcher's own sentinel proves something resupervises it, so the non-coherent-shell legacy keyboard is never killed without a restart. Verified with `python3 tests/test_handheld_keyboard_theme.py` (host, 15 cases) plus the existing `test_handheld_app_themes.py`, `test_handheld_theme_rendering.py`, `test_handheld_theme_default.py`, `test_handheld_theme_bundle.py`, `test_omarchy_theme_activation.py`, `test_omarchy_theme_transaction.py`, `test_theme_catalog.py` and `nix build .#handheld-theme-default .#handheld-theme-command`. Settings/notifications/chooser integration and the `--surface system` test flag are NOT part of this slice and remain open; do not read 3.3 as complete. No panel, touch, focus-preservation or reboot evidence exists for this slice; that needs the reserved board.
- [ ] 3.4 Inventory and implement installed app appearance adapters, reusing Foot config/OSC; verify safe session-scoped reload and per-app limitations with `python3 tests/test_handheld_app_themes.py`.

Proof: the named host rendering and app-adapter tests plus reviewed capture hashes. Build each changed derivation separately before image integration; these tests alone do not establish panel readability or touch correctness.

## 4. Expose all backgrounds and theme choices by touch

- [ ] 4.1 Add lazy bounded still-image previews, upstream format discovery, user overlays, crop/fit/fill/center/solid choices and per-theme selection memory; verify decode errors, deleted images, cache limits and no per-drag redecodes with `python3 tests/test_handheld_theme_backgrounds.py`.
- [ ] 4.2 Integrate scroll/swipe/tap theme and wallpaper previews with cancel/apply and the existing motion contract; verify selection, cancellation, reduced motion and recovery state with `python3 tests/test_handheld_theme_chooser.py` and reviewed host captures.
  - [x] 4.2.r Responsiveness slice only (branch `perf/theme-chooser-responsive`): the coordinator reproduced the Cover Flow chooser (task 4.2's own carousel) as real but so slow and quiet it looked dead -- 8s to the Preview page with "Preparing still preview" showing, 28s before the background carousel's thumbnails appeared, and the Rust log showing continuous `commit`/`frame-done` at ~15fps the whole time. Root cause: `main.rs`'s event loop forced `dirty = true` every iteration whenever `RendererCache::theme_thumbnails_pending()` was true, regardless of whether that iteration's `poll_theme_thumbnails()` actually reported a change -- a full Cairo scene render and Wayland commit at the compositor's own frame rate, continuously, competing with the decode thread for the K230's single in-order core, for the entire pending window. Fixed by deleting that block; retries/draining already happen unconditionally each iteration via the existing top-of-loop `poll_theme_thumbnails()`/`poll_theme_image()` calls, so nothing about correctness depended on it. Added immediate feedback in its place: a same-frame pressed-slice highlight (`theme_carousel::Carousel::pressed`), a rotating-arc loading spinner (`render.rs::spinner`) on unresolved thumbnail slots, the "Selected background" box, and a slice with a confirm request in flight, and an "Applying…" busy state on Apply -- all animated by a new `ThemeView::pulse_phase` that `main.rs`'s `THEME_PULSE_INTERVAL` (160ms, ~6/s) advances only while something is genuinely pending, never at rest. Fast thumbnails: (a) a hidden `--write-thumbnail-cache` CLI verb precomputes decoded `.rgba` thumbnails for every bundled theme's `preview.*` and background at both carousel geometries' sizes, mirrored into a `share/omarchy/thumbs/<name>/...` tree alongside (never inside -- see below) `share/omarchy/themes/<name>/...`; `nix/handheld-theme-default/default.nix` runs it at build time. (b) A persistent, content-hash-keyed, 24 MiB-bounded on-disk cache under `$XDG_CACHE_HOME/k230-shell/thumbs/` serves user themes and any cache miss, written through by the same background worker thread that already decoded off the render thread (unchanged, verified correct). (c) `RendererCache::poll_theme_image` (the "Selected background" still preview) now passes the generation's own root through to `BackgroundCache::render`, reusing `tools/theme_activate.py`'s already-written `background.cache` instead of a fresh full-source decode. A real mistake caught before landing: an earlier version put the build-time thumbnail sibling *inside* each theme's own directory, which `tools/theme_sources.py::source_digest`'s recursive hash silently absorbed, changing every generation identity `theme_activate.prepare()` computes and breaking `test_handheld_theme_bundle.py`'s pinned-inventory check for a real reason, not a test nuisance; the mirrored-tree layout fixes this and that test now passes clean. Evidence, including a same-branch before/after QEMU timing comparison under `cpulimit` throttle (cold-cache thumbnails-visible ~10.5s unfixed vs ~4.8s fixed at 50% throttle; a warm on-disk cache brings a repeat view to ~0.8s; zero idle commit/frame-done over a 5s untouched window after the fix, versus the old unconditional per-tick redraw) and loading/pressed-state screenshots, is in `docs/evidence/omarchy-themes/chooser-responsive/`. Proof: `cargo test --offline` (132 passed), `python3 tools/blob-scan.py` (exit 0, no inventory change), `nix build --no-link --print-out-paths --max-jobs 1 --cores 6 .#handheld-shell-rust .#handheld-theme-default .#handheld-theme-command .#card-shell`, `nix eval .#nixosConfigurations.k230-coherent-shell.config.system.build.toplevel.drvPath`, `python3 tests/rust_theme_chooser_qemu.py` plus `test_handheld_theme_bundle.py`/`test_handheld_theme_default.py`/`test_handheld_theme_rendering.py`/`test_handheld_theme_trial.py`/every `test_omarchy_theme_*.py`/`test_theme_background_metrics.py`/`test_theme_background_status.py`/`test_theme_catalog.py`/`test_theme_preferences.py` (all host, all pass). **QEMU proof only, and host-only for the Nix/pytest commands; no board time was used or requested for this slice** -- real-finger confirmation that the chooser now feels responsive, and a fresh-board first-boot timing of a bundled theme's build-time thumbnails, remain open and belong to task 4.2's own reserved-board gate (task group 5), not this slice.
  - [x] 4.2.r2 Staged-generation-copy fix (branch `fix/chooser-staged-thumbs`, from `origin/master` after 4.2.r landed): the board (system `xzb322jc`) confirmed the tap feedback, still-preview cache reuse and idle-redraw fix all work, but caught a real miss in 4.2.r's build-time thumbnails -- the Backgrounds carousel still took ~16s (40 commits in 5s at the pulse rate) because `theme_thumbnails::write_builtin_thumbnail`'s lookup mirrored the source's own *path* (`share/omarchy/themes/<name>/<rest>` -> `share/omarchy/thumbs/<name>/<rest>`), but `tools/theme_activate.py::prepare` stages a byte-for-byte copy of every background under `~/.local/state/omarchy/current/generations/<gen>/theme/backgrounds/...` -- the only path the chooser ever actually decodes -- which has no `themes` component to mirror. Fixed by keying both the build-time seed and the runtime disk cache identically, by the source's own content hash: the seed now ships as a flat, read-only `share/omarchy/thumbs-by-hash/<hash>-<variant>-<w>x<h>.rgba` directory, wired to the shell process as `K230_THEME_THUMBNAIL_SEED` by `nix/shell.nix` (same pattern as the existing `K230_THEME_DEFAULT_GENERATION`); a staged copy's bytes equal the pinned source's, so its hash still hits regardless of path. Verified for real (no synthetic fixture): staging `catppuccin` through the actual `theme_activate.prepare()` produces `generations/<gen>/theme/backgrounds/1-totoro.webp` (the exact background named in both reports), whose SHA-256 matches a real seed entry in the rebuilt package. New regression test: `theme_thumbnails::tests::resolve_finds_a_seed_thumbnail_for_a_staged_generation_copy_at_a_different_path`. A real-command (not synthetic-fixture), throttled-QEMU timing against the actual built `handheld-theme-command`/`handheld-theme-default` shows the background carousel settling in 2.45s at 50% cpulimit throttle (6 commits total), down from the board's reported ~16s/40-commits. Evidence: `docs/evidence/omarchy-themes/chooser-responsive/README.md`'s "Follow-up" section and its two new `timing-staged-fix-*.json` files. Proof: `cargo test --offline` (134 passed), `python3 tools/blob-scan.py` (exit 0), the same `nix build`/`nix eval` invocations as 4.2.r, `python3 tests/rust_theme_chooser_qemu.py`, `test_handheld_theme_bundle.py` and every `test_handheld_theme_*`/`test_theme_*`/`test_omarchy_theme_*` host suite (all pass). **QEMU and host proof only; the board was not touched for this fix** -- the same reserved-board gate 4.2.r left open still applies.
- [ ] 4.3 Add bounded muted video backgrounds with format diagnostics, visibility pause, still fallback and reduced-motion behavior; verify lifecycle/failure cases with `python3 tests/test_handheld_theme_backgrounds.py --video` and build the isolated `nix build .#handheld-wallpaper` output. Performance remains a physical gate.

Proof: the named host tests and narrow wallpaper build. Video suffix recognition or a first frame is not playback/performance acceptance.

## 5. Integrate and prove the actual handheld

- [ ] 5.1 Build changed client outputs individually using the existing `nix build .#touch-launcher` and `nix build .#card-shell`, then build `nix build .#nixosConfigurations.k230.config.system.build.toplevel`; record exact closure and default source identities. Serialize cross-builds.
- [ ] 5.2 Add a reserved-board theme trial/operator procedure with normal-session restoration, fixed nonsensitive results and baseline-versus-themed workload identity; verify its failure recovery protocol on the host with `python3 tests/test_handheld_theme_trial.py` before using it.
- [ ] 5.3 Run `python3 tools/handheld-theme-trial.py --candidate-manifest CANDIDATE --output NEW_EVIDENCE_DIR` on the reserved board, using the exact manifest produced in 5.1/5.2. Commit console observations, native screenshots and focused real-finger chooser/gesture video for a built-in dark theme, light theme and unchanged community theme. Observe cancel, rollback, icon changes and all applicable surface roles; retain unmet gates.
- [ ] 5.4 Run that trial's `--workload backgrounds` mode with paired static and supported video backgrounds; record combined CPU, RSS, decode/presentation and card input/frame budgets, covered-media pause and restoration. Pass the existing card budgets before enabling animated defaults; do not relabel fallback-only results as full playback.
- [ ] 5.5 Run that trial's `--workload reboot` procedure to record boot identities and observe remembered theme/wallpaper, fresh-home default and unavailable-source recovery. Physical console plus panel observations are required; a persistence unit test does not prove reboot behavior.

Proof: the exact recorded build invocations and reserved-board commands above, with source/candidate identities and restored normal session. `CANDIDATE` and `NEW_EVIDENCE_DIR` must be replaced with concrete paths in the committed operator record. No flash or full-image readback is inherently required by a userspace theme trial.

## 6. Publish evidence and close deliberately

- [ ] 6.1 Publish feature screenshots/video, source/license attribution, complete compatibility matrix and measured limitations; verify site links with `python3 scripts/build_site.py` and inspect the deployment for the exact landed revision.
- [ ] 6.2 After every required task and physical gate passes, validate, archive/sync and push the change; verify `openspec validate --all --strict`, then inspect the resulting master/CI/Pages revision. Keep this change open if any required consumer or hardware proof is missing.

Proof: `openspec validate --all --strict` and `python3 scripts/build_site.py`, followed by exact-revision CI and published URL inspection. Planning publication alone is not a shipped capability.
