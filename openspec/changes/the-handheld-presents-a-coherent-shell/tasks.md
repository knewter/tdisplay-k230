Dependencies: groups 1–3 can proceed after this revised proposal lands. Group 4 consumes groups 1–3 plus the selected `the-shell-has-a-card-composition-plan` interface and `the-shell-manages-apps-as-cards` live-surface, focus, and physical-cost gates. Group 5 requires a board reservation and the installed opt-in integrated image. The card sibling's bounded direct drag/close is a prerequisite; this successor owns coordinated motion among shell surfaces. No task below is claimed complete by this proposal.

The `tests/test_shell_routes.py`, `tests/test_shell_gestures.py`, `tests/test_shell_motion.py`, `tests/test_device_settings.py`, and `tests/test_notification_center.py` files, `tools/shell-motion-trace.py`, and `.#handheld-settings`/`.#handheld-notifications` attributes are **new interfaces to create** in the named implementation tasks, not presently runnable commands. Their invocations define narrow future proof. Existing `.#touch-launcher`, closure, OpenSpec, and capture tools are current commands.

## 1. Shared shell frame and route contract (host)

- [ ] 1.1 Implement the common palette/type/spacing/target tokens and separate Apps, Windows, Keyboard, System, Back, and Home actions at output scale 1, with final Home→Apps and named Terminal recovery; verify `python3 tests/test_shell_routes.py --case labels --case back-stack --case home --case terminal-recovery` checks target geometry and focus restoration.
- [ ] 1.2 Implement launcher vertical finger tracking, bounded flick/coast/tap-to-stop, optional horizontal section swipe, and a visible section cue; keep Previous/Next only as secondary accessible routes. Verify `python3 tests/test_shell_gestures.py --case list-drag --case flick-stop --case section-snap --case button-fallback`.
- [ ] 1.3 Implement press feedback and a deliberate cancellable long-press context sheet for app/card items, with no tap on held release; verify `python3 tests/test_shell_gestures.py --case press --case hold --case move-cancel --case back-cancel --case second-contact`.
- [ ] 1.4 Add loading, empty, stale, failure, cancellation, and reduced-motion variants without hiding recoverable actions behind wvkbd; verify `python3 tests/test_shell_routes.py --case states --case keyboard --case reduced-motion`.
- [ ] 1.5 Package fresh-home shell defaults and an opt-in rollback flag; verify `nix build .#touch-launcher` (derivation proof only, no panel or touch claim).

## 2. Truthful Settings (host)

- [ ] 2.1 Implement System hub with distinct Settings and Notifications destinations, and settings capability-state adapters for network, display brightness, keyboard, and motion. Show unavailable/read-only/pending states when the installed interface cannot support an action; verify `python3 tests/test_device_settings.py --case capability-states --case no-battery --case fresh-home`.
- [ ] 2.2 Add finger-tracked Settings scrolling, bounded coast, tap-to-stop, and no accidental toggle on stop; verify `python3 tests/test_device_settings.py --case scroll --case flick-stop --case no-toggle`.
- [ ] 2.3 Connect confirmation and completion/error feedback for existing restart/power helper without optimistic state; verify `python3 tests/test_device_settings.py --case restart-cancel --case denial --case confirmed`.
- [ ] 2.4 Build the narrow settings package before image integration; verify `nix build .#handheld-settings` (derivation proof only).

## 3. Notification preview and history (host)

- [ ] 3.1 Implement bounded event records for shell/system and deliberately integrated apps, source validation, priority policy, deduplication, and safe body defaults; verify `python3 tests/test_notification_center.py --case priority --case privacy --case retention --case unknown-source`.
- [ ] 3.2 Implement non-focus-taking preview, System count, History, action validation, dismissal, and empty/target-gone/critical states; verify `python3 tests/test_notification_center.py --case preview-timeout --case typing-focus --case action-gone --case dismiss-all --case critical`.
- [ ] 3.3 Add finger-tracked History scrolling and reversible sideways item-dismiss swipe with an explicit cue; verify `python3 tests/test_notification_center.py --case history-flick-stop --case swipe-cancel --case swipe-dismiss --case critical-retained`.
- [ ] 3.4 Build the narrow broker/client package with Nix-provided defaults; verify `nix build .#handheld-notifications` (derivation proof only). A standard desktop notification adapter remains a separately reviewable extension after these sources work.

## 4. Coordinated scene motion (host, after sibling gates)

- [ ] 4.1 Consume the card owner's published lifecycle, privacy, and fallback interface so Windows shows eligible live cards only when proven, otherwise names unavailable/private metadata honestly; verify `python3 tests/test_shell_routes.py --case live-card-gate --case private-card --case close-refused`.
- [ ] 4.2 Implement one scene-state owner on the selected opt-in Sway/wlroots route for app→card geometry, horizontal deck position/snap, card→app expansion, launch/return, z-order, and focus handoff. Consume the sibling card lifecycle rather than reimplementing close; verify `python3 tests/test_shell_motion.py --case shrink-live --case adjacent-snap --case expand-live --case launch-return --case focus`.
- [ ] 4.3 Make movement interruptible/reversible from the current visible state, including Back, second touch, unmap, and close refusal; preserve private placeholders and no blank/misowned frame. Verify `python3 tests/test_shell_motion.py --case reverse --case retarget --case unmap --case refusal --case private-no-flash`.
- [ ] 4.4 Coordinate launcher/Settings/History/preview entry and exit around stable anchors, with app and keyboard focus retained; implement a gesture-owner trace that proves app scroll/text selection and keyboard controls beat an unqualified edge gesture. Verify `python3 tests/test_shell_motion.py --case overlay-anchor --case notice-during-drag --case keyboard-focus` and `python3 tests/test_shell_gestures.py --case app-scroll-conflict --case edge-qualified --case keyboard-edge`.
- [ ] 4.5 Add trace IDs across touch, scene state, damage/commit, frame-done, output-presented, and process CPU/memory; declare board p95/p99 input-to-visible and frame budgets before measuring. Verify `python3 tools/shell-motion-trace.py --self-test` and `python3 tests/test_shell_motion.py --case trace-order --case reduced-motion` (host trace schema only, not panel presentation).
- [ ] 4.6 Build the integrated system closure only after narrow packages and fixtures pass; verify `nix build .#nixosConfigurations.k230.config.system.build.toplevel` (cross-build proof only, not board behavior).

## 5. Physical acceptance and evidence (board reservation required)

- [ ] 5.1 On installed glass, record readable Apps/Windows/Settings/History, all-edge real-finger targets, Back/Home, and keyboard viewport without secrets; verify `python3 tools/capture-feature.py coherent-shell --provenance real-touch --duration 45 --description 'Shell surfaces and recovery on glass' --output-dir docs/evidence/coherent-shell` produces a reviewed camera recording, and separately run `flock /tmp/k230-board.lock ./tools/console.py /dev/ttyACM0 --wait=3 'systemctl is-active shell'` for an actual sanitized console result committed with the capture. A native screenshot or operator-declared camera provenance alone is insufficient.
- [ ] 5.2 Record real-finger app→shrinking live card→adjacent swipe/snap→expand, reversal mid-transition, upward throw and refusal/timeout recovery; verify `python3 tools/capture-feature.py coherent-cards --provenance real-touch --duration 60 --description 'Live card motion, reversal and close recovery on glass' --output-dir docs/evidence/coherent-cards` and cite the sibling's committed live-source/focus/cost evidence. An injected drag or endpoint screenshot does not complete this task.
- [ ] 5.3 Record launcher and Settings finger scroll/flick/tap-to-stop, cancellable long press, app-scroll/keyboard edge conflicts, and fallback controls; verify `python3 tools/capture-feature.py coherent-gestures --provenance real-touch --duration 60 --description 'Touch-native browsing and gesture ownership on glass' --output-dir docs/evidence/coherent-gestures`, with an operator outcome table and no accidental activations. Keep edge entry disabled if it conflicts.
- [ ] 5.4 Record ordinary preview while typing, History scroll/side-swipe cancellation and committed dismiss, critical recovery, and a failed settings action; verify `python3 tools/capture-feature.py coherent-notifications --provenance real-touch --duration 60 --description 'Notification motion, focus and settings feedback on glass' --output-dir docs/evidence/coherent-notifications`, with sanitized console timestamps and an outcome table committed under that directory.
- [ ] 5.5 Measure representative two-app live drag, app/card reverse, launcher coast, and notice arrival with `python3 tools/shell-motion-trace.py --board --output docs/evidence/coherent-shell/motion.json` under the reserved board session; correlate input→scene commit→output presentation and camera-visible motion, report p50/p95/p99, dropped/blank frames, CPU/RSS, renderer and actual output cadence. Record failure against the declared budget rather than claiming the panel mode or frame-done is presentation proof.
- [ ] 5.6 Validate after evidence and keep any unmet physical task open; verify `openspec validate the-handheld-presents-a-coherent-shell --strict` and `python3 tools/work-status.py`. Do not archive from host-only or QEMU proof.
