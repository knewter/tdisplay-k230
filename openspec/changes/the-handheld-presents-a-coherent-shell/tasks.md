All tasks remain unchecked. Groups 1–3 can start after this revised proposal lands. Group 4 consumes the selected `the-shell-has-a-card-composition-plan` interface and `the-shell-manages-apps-as-cards` live-source/focus/cost gates. Group 5 requires a reserved board and installed opt-in image. The current bar is a separate rollback session, never a final navigation element.

The final launcher/drawer/shade/Settings UI client targets Rust. Current C source is a development reference/rollback, while Sway remains the compositor and current Python Settings/notification helpers remain separate services. The Rust probe in group 0 is a feasibility gate, not completion of any gesture, Settings, notification, or physical task. Compare it with the independent Qt Quick software probe before selecting a final rendering toolkit.

Some named host files and narrow packages now exist as source checkpoints; each unchecked case below still requires its own actual implementation and passing command. `tests/test_rust_shell_probe.py`, `tests/test_shell_motion.py` and `tools/shell-motion-trace.py` are **interfaces to create**, not current proof. Existing Nix, OpenSpec, and capture commands are current. No host result substitutes for the physical gates.

## 0. Rust client target and architecture gate (probe, not final UI)

- [ ] 0.1 Pin a minimal Rust Wayland/layer-shell client and software `wl_shm` renderer with `Cargo.lock`, private route request and touch/cancel instrumentation; verify `python3 tests/test_rust_shell_probe.py --case route-timeout --case configure-resize --case buffer-release --case touch-cancel`, then `nix build .#handheld-shell-rust-probe --dry-run`. Record the exact target/closure and compare dependency choices with the Qt Quick software probe. Dry-run is an evaluation gate, not a cross-build.
- [ ] 0.2 Cross-build `nix build .#handheld-shell-rust-probe --no-link --print-out-paths --max-jobs 1 --cores 4` only in the reserved build slot. On a reserved board, opt in to the probe and record 568×1232 map/unmap, distinguishable software frame, real touch/move/cancel, bounded IPC acknowledgement, cold start, RSS, CPU and touch-to-commit/present cadence against the C reference and Qt probe. Keep this task open until the physical trace and camera evidence are committed. Do not install the probe as the normal session from a build alone.

## 1. Gesture Home and installed-app drawer (host)

- [ ] 1.1 Implement final Home-as-live-deck routing, empty-deck cue, bottom app→deck swipe and continued deck→drawer pull, with no persistent rail/footer in the final session; verify `python3 tests/test_shell_routes.py --case app-home --case empty-deck --case deck-drawer --case no-final-bar`.
- [ ] 1.2 Implement drawer vertical finger tracking, bounded flick/tap-to-stop and optional horizontal sections; verify `python3 tests/test_shell_gestures.py --case drawer-drag --case flick-stop --case section-snap --case cancel-below-threshold`.
- [ ] 1.3 Implement tap launch, press cue and cancellable long-press context sheet without tap-after-hold; verify `python3 tests/test_shell_gestures.py --case tap-launch --case hold-cue --case move-cancel --case back-cancel --case second-contact`.
- [ ] 1.4 Resolve desktop-entry Icon through installed icon themes with fixed drawer/card/notification sizes, bounded decode cache, named fallback, and private-card neutral placeholder; verify `python3 tests/test_shell_icons.py --case theme --case missing --case cache-bound --case notification-icon --case private-no-identity`.
- [ ] 1.5 Provide small contextual gesture cues, Help and an opt-in accessibility aid while retaining the old persistent-bar session only as rollback; verify `python3 tests/test_shell_routes.py --case cues --case aid --case rollback-separated --case terminal-recovery`.
- [ ] 1.6 Build the drawer package and fresh-home defaults; verify `nix build .#touch-launcher` (derivation proof only, no touch/panel claim).

## 2. Top shade and truthful Settings (host)

- [ ] 2.1 Implement top-edge shade reveal over the current scene, notification history/count, and Settings entry; verify `python3 tests/test_shell_routes.py --case top-shade --case shade-settings --case prior-focus`.
- [ ] 2.2 Implement qualified side-edge contextual Back for shell sheet/Settings/shade/drawer/keyboard, without injecting a universal Back key into apps; verify `python3 tests/test_shell_gestures.py --case context-back-order --case app-back-not-synthesized --case keyboard-owns-edge`.
- [ ] 2.3 Implement capability-state adapters for network, brightness only when controllable, keyboard and motion; show unavailable/read-only/pending states and no battery UI. Verify `python3 tests/test_device_settings.py --case capability-states --case no-battery --case fresh-home`.
- [ ] 2.4 Add finger-tracked Settings scrolling and confirmed/cancelled/failed restart-power feedback; verify `python3 tests/test_device_settings.py --case scroll --case flick-stop --case restart-cancel --case denial`.
- [ ] 2.5 Build the narrow settings package; verify `nix build .#handheld-settings` (derivation proof only).

## 3. Notification preview and history (host)

- [ ] 3.1 Implement bounded events from shell/system and deliberately integrated apps, source validation, priority, privacy and deduplication; verify `python3 tests/test_notification_center.py --case priority --case privacy --case retention --case unknown-source`.
- [ ] 3.2 Implement non-focus-taking top preview, shade history/action/dismissal, empty and target-gone/critical states; verify `python3 tests/test_notification_center.py --case preview-timeout --case typing-focus --case action-gone --case critical`.
- [ ] 3.3 Implement history drag/flick/tap-to-stop and reversible sideways item dismissal; verify `python3 tests/test_notification_center.py --case history-flick-stop --case swipe-cancel --case swipe-dismiss --case critical-retained`.
- [ ] 3.4 Build the narrow notification broker/client with Nix defaults; verify `nix build .#handheld-notifications` (derivation proof only). A desktop notification adapter remains a separately reviewable extension.

## 4. Coordinated compositor scene (host, after sibling gates)

- [ ] 4.1 Consume the sibling's live-source, privacy, graceful close and focus lifecycle, showing only eligible live cards and neutral private/unavailable placeholders; verify `python3 tests/test_shell_motion.py --case live-gate --case private-no-icon --case close-refused`.
- [ ] 4.2 On the selected opt-in Sway scene route, coordinate bottom app→deck, deck→drawer, card→app and top shade→Settings geometry, z-order, clipping and focus as one state; verify `python3 tests/test_shell_motion.py --case shrink-live --case drawer-rise --case shade-descend --case expand-live --case focus`.
- [ ] 4.3 Support reverse/retarget from current visible geometry on Back, new drag, second contact, unmap and close refusal with no blank/misowned frame; verify `python3 tests/test_shell_motion.py --case reverse --case retarget --case unmap --case refusal --case private-no-flash`.
- [ ] 4.4 Define touch ownership for bottom Home/drawer, top shade, side Back, keyboard, deck and app content; test app scroll/text selection and keyboard conflict before enabling edges. Verify `python3 tests/test_shell_gestures.py --case app-scroll-conflict --case bottom-qualified --case top-qualified --case side-qualified --case keyboard-inset`.
- [ ] 4.5 Add touch→scene damage/commit→frame-done→output-presented trace IDs, reduced-motion parity, process CPU/memory sampling, and declared board p95/p99 budgets; verify `python3 tools/shell-motion-trace.py --self-test` and `python3 tests/test_shell_motion.py --case trace-order --case reduced-motion` (host trace schema, not panel presentation).
- [ ] 4.6 Build the integrated closure only after narrow packages and fixtures pass; verify `nix build .#nixosConfigurations.k230.config.system.build.toplevel` (cross-build proof only).

## 5. Physical acceptance and evidence (board reservation required)

- [ ] 5.1 On glass, record bottom Home→live deck, empty deck→drawer, drawer→app, top shade→Settings, contextual Back, keyboard escape, and no permanent final bar/footer; verify `python3 tools/capture-feature.py coherent-shell --provenance real-touch --duration 60 --description 'Gesture Home drawer shade and recovery on glass' --output-dir docs/evidence/coherent-shell`, plus `flock /tmp/k230-board.lock ./tools/console.py /dev/ttyACM0 --wait=3 'systemctl is-active shell'` saved as sanitized console evidence. Inspect camera and trace separately; operator-declared provenance alone is insufficient.
- [ ] 5.2 Record app→shrinking live card→adjacent swipe/snap→expand, reverse mid-transition, upward throw and refusal/timeout; verify `python3 tools/capture-feature.py coherent-cards --provenance real-touch --duration 60 --description 'Card motion and close recovery on glass' --output-dir docs/evidence/coherent-cards` and cite the sibling live-source/focus/cost evidence. An injected gesture or endpoint screenshot does not complete this task.
- [ ] 5.3 Record real-finger drawer/Settings scrolling, icon/text/fallback readability, long-press cancellation, app/keyboard conflict and accessibility aid discovery; verify `python3 tools/capture-feature.py coherent-gestures --provenance real-touch --duration 60 --description 'Gesture ownership and drawer discovery on glass' --output-dir docs/evidence/coherent-gestures` with an outcome table and no accidental activations. Keep the old bar rollback if any mandatory route fails.
- [ ] 5.4 Record preview while typing, shade scroll/side-swipe cancellation and committed dismiss, critical recovery and failed settings action; verify `python3 tools/capture-feature.py coherent-notifications --provenance real-touch --duration 60 --description 'Shade notification and settings motion on glass' --output-dir docs/evidence/coherent-notifications` with sanitized timestamps and outcome table.
- [ ] 5.5 Measure two-app live drag, app/deck reversal, drawer rise and shade arrival with `python3 tools/shell-motion-trace.py --board --output docs/evidence/coherent-shell/motion.json` under the board reservation; correlate input→scene commit→output presentation and camera-visible movement, report p50/p95/p99, blank/missed frames, CPU/RSS, renderer and actual cadence. Record failed budgets honestly.
- [ ] 5.6 Validate after committed evidence and leave unmet physical tasks open; verify `openspec validate the-handheld-presents-a-coherent-shell --strict` and `python3 tools/work-status.py`. Do not archive from host-only or QEMU proof.
