## 1. Evidence inventory and journey map

- [x] 1.1 Build the source-of-truth inventory for boot handoff, shell controls, launcher/catalog, keyboard, cards, and video; link each observation to its committed report or capture and label host/native/injected/camera/real-finger evidence. Use `rg -n "UNVERIFIED|injected|physical|camera|native" docs/evidence` to locate candidate sources, then inspect the cited artifacts and record the review in `docs/research/handheld-ux/evidence-review.md`; search matches alone are not proof.
- [x] 1.2 Write the outside-in first-use, repeat-use, keyboard-visible, video, error, and recovery flows with user goals, visible states, escape routes, and accessibility checks. Verify the delivered flow matrix covers every requirement in this change's `specs/docs/handheld-ux-plan/spec.md` (host document review).
- [x] 1.3 Create `docs/research/handheld-ux/flow-matrix.md`, `issue-ledger.md`, and `evidence-index.md` with required fields for task, visible result, severity, owner, dependency, evidence class, citation path, and open gate; reconcile ownership with `the-shell-has-a-card-composition-plan`, `the-shell-manages-apps-as-cards`, `touch-launcher-gestures-overview`, and `the-shell-plays-network-video`. After implementing task 1.4, verify every citation path exists or is explicitly named `UNVERIFIED` with `python3 tools/verify-ux-plan.py docs/research/handheld-ux/flow-matrix.md docs/research/handheld-ux/issue-ledger.md docs/research/handheld-ux/evidence-index.md` (host proof).
- [x] 1.4 Add the small allowlisted `tools/verify-ux-plan.py` verifier for required table fields and citation existence; it must accept explicit `UNVERIFIED` rows and reject missing owner, evidence class, or acceptance fields. Verify with `python3 tools/verify-ux-plan.py --self-test` (host proof).

## 2. Shared visual and interaction contract

- [x] 2.1 Define portrait visual tokens for text roles, spacing, contrast, focus/pressed/error states, minimum touch geometry, and keyboard-visible layout, with a method to record panel-pixel and camera limitations. Verify the contract includes a native screenshot check and a separate optical/real-finger gate (host document review).
- [x] 2.2 Define navigation ownership for Apps, Windows/overview, cards, Back, Home, Keyboard, System, Stop, Help, and focus return, including empty/stale/loading/failure/EOF/cancelled states. Verify every route has a visible fallback and an owning successor proposal (host document review).
- [x] 2.3 Define motion and accessibility acceptance in `docs/research/handheld-ux/interaction-contract.md`: distinguish release-animation budgets from continuous finger tracking, preserve required card interactions under reduced motion, and cover readable status/focus. Create annotated current-versus-recommended wireframes and a navigation storyboard in that directory; review against existing native captures without claiming new pixels shipped. Verify required fields and references with `python3 tools/verify-ux-plan.py docs/research/handheld-ux/interaction-contract.md` and record coordinator visual review in `docs/research/handheld-ux/design-review.md`. Extend the verifier schema for the contract as needed; it does not assess design quality.

## 3. P0/P1 successor proposals and parallel ownership

- [x] 3.1 Derive P0/P1 successors from the completed ledger: reuse an existing owner when it covers the gap, and create a new bounded proposal only for an uncovered issue such as onboarding, visual/state consistency, or accessibility. Verify each selected proposal names the layer owner, rollback path, dependency, and board command before implementation (host OpenSpec review).
- [ ] 3.2 Route each selected P0/P1 finding to an existing owner or a new bounded successor; preserve network-video lifecycle and card architecture ownership. Validate all resulting proposals with `openspec validate --all --strict`, merge/push them to master, and record their commits and dependencies in the issue ledger. If no uncovered P0/P1 finding exists, record that conclusion rather than inventing work.
- [x] 3.3 Coordinate P1 card composition and app-card lifecycle with the existing proposals, adding only missing visual/navigation contract work; schedule source/model work in parallel and physical gesture/readability proof serially. Verify `openspec status --change the-shell-has-a-card-composition-plan --json` and the app-card proposal status are reviewed before landing any successor (host OpenSpec review).
- [x] 3.4 Keep rotation and advanced visual effects as explicit deferred questions until source or board evidence exists. Verify the ledger retains `UNVERIFIED` markers rather than creating an unsupported implementation task (host document review).

## 4. Acceptance and integration

- [x] 4.1 Run host source, catalog, launcher, menu, and video-state checks relevant to the ledger; record which checks are deterministic and which do not establish physical usability. Verify with `python3 tests/test_launcher_navigation.py && python3 tests/test_touch_menu.py && python3 tests/test_window_catalog.py && python3 tests/test_desktop_catalog.py` (host proof only).
- [x] 4.2 Decide which findings need additional board observation after reviewing existing accepted evidence. For a needed boot capture, reserve serial and run `flock /tmp/k230-board.lock python3 tools/capture-boot.py --dev /dev/ttyACM0 --out /tmp/k230-ux-boot.txt --seconds 120`; for a needed physical interaction capture run `python3 tools/capture-feature.py ux-review --provenance real-touch --duration 30 --description 'Observed handheld flow for UX review' --output-dir /tmp/k230-ux-capture`. Arrange real interaction before labelling footage real-touch, sanitize and review before committing selected artifacts. If observation is unavailable or unnecessary, record the reason and explicit unknowns; that is an audit outcome, not hardware acceptance. Do not repeat already accepted keyboard/Home trials without a new concern.
- [ ] 4.3 Validate and publish the complete planning change with unchecked tasks preserved for unperformed evidence. Verify with `openspec validate the-handheld-has-a-coherent-ux-plan --strict`, `git diff --check -- openspec/changes/the-handheld-has-a-coherent-ux-plan`, and `python3 tools/work-status.py` (host proof).
